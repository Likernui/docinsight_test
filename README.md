# DocInsight

Десктопное приложение для извлечения сущностей и семантического поиска по корпусу документов.

## Назначение

Автоматическое извлечение структурированных данных (ФИО, научные руководители, источники, технологии и др.) из документов:
- Отчёты (DOCX)
- Документы (PDF)
- Сканы (PNG, JPG)

## Установка

```bash
pip install -r requirements.txt
```

## Запуск тестов

### Консольный тест
```bash
python test_document_loader.py
```

### GUI тест
```bash
python test_gui.py
```

## Архитектура

*Будет добавлено позже*

## Модули

- `src/text_extractor.py` — ✅ **Готово**: извлечение текста из DOCX, PDF, изображений
- `src/preprocessor.py` — ✅ **Готово**: очистка и разбиение текста на фрагменты
- `src/indexer.py` — ⏳ В разработке: создание векторного индекса
- `src/semantic_search.py` — ⏳ В разработке: семантический поиск
- `src/entity_extractor.py` — ⏳ В разработке: извлечение сущностей (NER + regex + GLiNER)
- `src/aggregator.py` — ⏳ В разработке: агрегация результатов
- `src/result_generator.py` — ⏳ В разработке: генерация итогового DOCX

## Реализованный функционал

### ✅ Пункт 2.1: Извлечение текста из DOCX и PDF

**DocxExtractor:**
- Извлечение текста из параграфов
- Извлечение текста из таблиц
- Извлечение текста из колонтитулов (header/footer)
- Извлечение текста из сносок (footnotes)

**PdfExtractor:**
- Извлечение текста постранично
- Использование библиотеки PyMuPDF

### ✅ Пункт 2.2: OCR для сканированных документов

**ImageExtractor:**
- Распознавание текста с изображений
- Поддержка множественных языков (ru, en по умолчанию)
- Ленивая загрузка OCR модели
- Использование библиотеки EasyOCR

### ✅ DocumentLoader

- Автоматическое определение формата файла
- Поддержка форматов: `.docx`, `.pdf`, `.png`, `.jpg`, `.jpeg`
- Загрузка одного файла или пакетная загрузка
- Обработка ошибок без прерывания процесса
- Callback для отображения прогресса

### ✅ Предобработка текста (`preprocessor.py`)

**TextCleaner:**
- Удаление переносов слов через дефис
- Схлопывание множественных переносов строк (3+ → 2)
- Схлопывание множественных пробелов
- Удаление спецсимволов и мусора

**TextChunker:**
- Разбиение по параграфам (по умолчанию)
- Разбиение по предложениям
- Фиксированное разбиение
- Настраиваемое перекрытие (overlap) между чанками

**TextPreprocessor:**
- Полный пайплайн: очистка + chunking
- Обработка одного документа или пакета
- Структура `TextChunk` с метаданными (файл, индекс, доп. информация)

## Тестирование

### GUI для тестирования (`test_gui.py`)

- Выбор нескольких файлов одновременно
- Загрузка файлов по одному (накопительный режим)
- Просмотр полного текста каждого файла
- Переключение между файлами через выпадающий список
- Статистика по всем файлам (символы, слова, строки)
- Индикация прогресса обработки

### Консольный тест (`test_document_loader.py`)

- Автоматическая проверка всех форматов
- Вывод статистики по каждому файлу
- Тестирование OCR функциональности

### Тест предобработки (`test_preprocessor.py`)

- Проверка очистки текста (TextCleaner)
- Проверка разбиения на чанки (TextChunker)
- Демонстрация стратегий chunking'а
- Визуализация результатов в GUI
